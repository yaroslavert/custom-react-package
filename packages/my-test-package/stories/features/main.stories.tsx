import { TestTable } from '../../src/';
import { type Meta } from '@storybook/react';

const meta: Meta = {
  title: 'Features/debug',
};

export default meta;

export const StateDefault = () => <TestTable />;
